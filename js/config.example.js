/* ================================================================
   SJNAM — TEMPLATE KONFIGURASI SUPABASE
   ================================================================
   File ini AMAN untuk diupload ke GitHub — hanya berisi placeholder,
   bukan kredensial asli.

   CARA PAKAI:
   1. Salin file ini menjadi js/config.js (di folder yang sama)
   2. Ganti SUPABASE_URL dan SUPABASE_ANON_KEY di bawah dengan
      kredensial asli dari dashboard Supabase Anda
      (Project Settings → API → Project URL / anon public key)
   3. js/config.js TIDAK akan ter-upload ke git (sudah di .gitignore)

   Tanpa file js/config.js, aplikasi tetap bisa dibuka, tapi fitur
   Cloud Sync (Supabase) tidak akan berfungsi sampai Anda mengisi
   konfigurasi lewat menu Cloud Sync di dalam aplikasi secara manual.
   ================================================================ */
window.SJNAM_CONFIG = {
  SUPABASE_URL: 'https://YOUR-PROJECT-REF.supabase.co',
  SUPABASE_ANON_KEY: 'YOUR-ANON-KEY-HERE'
};
